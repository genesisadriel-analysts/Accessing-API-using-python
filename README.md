# Accessing-API-using-python
This project demonstrates how to interact with REST APIs using the Python Requests library. It covers performing GET and POST requests, inspecting HTTP status codes, and managing request/response headers. The repository includes a practical example of fetching and saving image data (PNG) from a remote server using file handling in Python.
